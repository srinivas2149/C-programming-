#include <stdio.h>
int main() {
    int num = 1; // Start from 1

    while (num <= 5) { // Continue while num is less than or equal to 5
        printf("%d\n", num); // Print current number
        num++; // Increment number by 1
    }

    return 0;
}
